{
  "name": "gym-tracker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:api": "npm --prefix src/api run dev",
    "start:web": "npm --prefix src/web run dev",
    "start": "concurrently \"npm run start:api\" \"npm run start:web\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}